## Приложение для просмотра и редактирования траекторной информации (Java, Swing)

### Требования:
* Maven
* SDK 1.8

### Логирование
Лог-файл `appLog.log` находится в директории `log/`

### Сборка

```mvn clean compile assembly:single``` (из идеи запустить с помощью `Ctrl + Enter`)